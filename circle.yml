dependencies:
  pre:
    - curl https://raw.githubusercontent.com/pytlesk4/stoplight-todos/master/prism.sh | sh

test:
  override:
    - prism test -s https://api.stoplight.io/v1/versions/qFdgE5KMaHpFAWpqo/export/stoplight.json
